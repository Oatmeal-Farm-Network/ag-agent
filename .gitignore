# Ignore Python virtual environments
.venv

# Don't copy the virtual environment
agenv

# Don't copy the git repository or Dockerfile itself
.git
.gitignore
.dockerignore

# Don't copy cache
__pycache__
.cache
.cache.sqlite

# Don't copy the secrets file (it will be injected at runtime)
.env

# Don't copy notebooks or documentation
*.ipynb
README.md

#Node Modules
frontend/node_modules/
frontend/.DS_Store